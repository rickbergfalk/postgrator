-- Script is for PostgreSQL-- -- We are going to drop these so hard!-- table drops are in reverse order of Migration 003.do -- because of potential dependencies and things.DROP TABLE Users;DROP TABLE Account;-- see comments work just fine. Comment away!